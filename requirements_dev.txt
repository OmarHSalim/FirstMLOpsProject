pymongo
pymongo[srv] #[srv= server] connecting to MongoDB Atlas clusters using the SRV DNS record format
dnspython
pandas
numpy
ensure
#pytest # test different cases: It is only required for dev env

# Note: This for developing in local env and writing libraray for testing

# dev requirements -
pytest==7.1.3 #  is a testing framework for Python that makes it easy to write simple and scalable test cases.
tox==3.25.1 # Tox is a tool for automating the testing of Python packages across multiple Python environments. 
# It helps ensure that your code works correctly across different versions of Python and dependencies.

# Linting env: formatting. For example, for-loop needs 4 spaces (indentations), otherwise, get warning
black==22.8.0 # is a Python code formatter that automatically reformats Python code to follow a consistent style.
flake8==5.0.4 # Checking code 
# Flake8 is a tool for enforcing style and quality standards in Python code. It combines several Python linting tools (including PyFlakes, pycodestyle, and Ned Batchelder’s McCabe script) 
# to check Python code against coding standards defined in the PEP 8 style guide and to detect various errors and potential problems.
mypy==0.971
# Mypy is an optional static type checker for Python. It aims to combine the benefits of dynamic typing and static typing by allowing 
#developers to annotate their Python code with type hints, which can then be checked statically for errors by the mypy tool.

-e . # install packages in local repository